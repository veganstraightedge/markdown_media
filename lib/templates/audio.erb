<figure id="<%= id %>">
  <audio controls>
    <source src="<%= embed_id %>" />
  </audio>

  <% unless caption.to_s.empty? %>
    <figcaption class="caption audio-caption"><%= MarkdownMedia.render_markdown caption %></figcaption>
  <% end %>
</figure>
