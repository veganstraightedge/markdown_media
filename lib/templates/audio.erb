<figure id="<%= id %>">
  <audio controls>
    <source src="<%= embed_id %>" />
  </audio>

  <% unless caption.to_s.empty? %>
    <%# TODO render_markdown %>
    <figcaption class="caption audio-caption"><%= caption %></figcaption>
  <% end %>
</figure>
