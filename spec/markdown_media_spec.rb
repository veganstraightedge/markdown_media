require "spec_helper"

RSpec.describe MarkdownMedia do
  it "has a version number" do
    expect(MarkdownMedia::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
